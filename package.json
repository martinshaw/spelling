{
  "name": "spelling",
  "version": "0.0.1",
  "description": "Improve your English spelling with this beautiful web app",
  "scripts": {
    "start": "rimraf .parcel-cache && parcel src/index.html",
    "build": "rimraf .parcel-cache && parcel build src/index.html --public-url /spelling",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Martin Shaw <hello@martinshaw.co>",
  "license": "GPL-3.0-only",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "tailwind-colors-scss": "^1.0.0"
  }
}
